// This file is no longer used - functionality moved to App.tsx
// Keeping this file to avoid compilation errors, but it's not imported anywhere

import React from 'react';

const MainLayout: React.FC = () => {
    return (
        <div>
            {/* Placeholder component - not used */}
        </div>
    );
};

export default MainLayout;